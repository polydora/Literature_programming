---
title: "Пратикум по основам языка R"
author: "Вадим Хайтов"
output:
  html_document:
    toc: yes
    toc_depth: 2
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
library("knitcitations")
cleanbib()

```


## **Задание 1.** Получение справки

Какой параметр функции `metaMDS()` отвечает за выбор меры индекса разлчия между сравниваемыми объектами.  


## **Задание 2.** R как калькулятор, математические операции


Напишите код, который позволит найти решение этого примера

$$
\log_2 \left(\frac{2 \cdot arcsin(\sqrt{0.5}) \cdot \frac{180}{\pi}-\sqrt[4]{11} }{(10 + e^2) \cdot 50}\right) =
$$



## **Задание 3.** Переменные и оформление кода

1. Cкачайте к себе на компьютер датасет [catsM.xlsx](data/catsM.xlsx). Поместите его в папку "data". 

2. Скопируйте приведнный ниже код в окно скрипта RStudio.

3. Перепишите код так, чтобы он полностью выполнялся (надо расставить строки в правильном порядке).

Если все правильно, то после выделения всех строк кода и нажатия Ctrl+Enter должно все работать без сообщения об ошибках. 

Суть происходящего в этом коде совершенно не важна, важно, чтобы все работало.

```{r, echo=TRUE, eval=FALSE}
summary(Model)
plot(Model, page = 1)
cat <- read_excel("data/catsM.xlsx") # Здесь надо указать путь к файлу на вашем компьютере 
Model <- lm(Hwt ~ Bwt, data = cat)
par(mfrow = c(2, 2))
library(readxl)
str cat
install.packages("readxl")



```



## **Задание 4.** Векторы{.smaller} 

Выполните следующий код

```{r, echo = T, eval=FALSE}
n <- round(runif(1, min = 10, max = 50),0)
vector <- round(rnorm(n, 0, 1), 0)
```

Напишите код, который даст следующие характеристики полученного вектора.

1. Количество чисел в нем.   
2. Среднее значение в этом векторе.   
3. Срднеквадратичное отклонение для данного вектора.
3. Отношение количества чисел равных нулю  к общему количеству чисел в данном ряду (можно использовать только одну функцию).




## **Задание 4.** Датафреймы{.smaller}

Скопируйте и выполните код, который создаст датафрейм под названием `df`

*Это абсолютно бессмысленный, учебный, набор данных!*

```{r echo = TRUE}
df <- data.frame(Var_1 = rep(letters[1:9], each =2), Var_2 = seq(1, 100, length.out = 18), Var_3 = NA)
df
```


Напишите код, который изменит названия переменных следующим образом

```{r}
names(df) <- c("Var_One", "Var_Two", "Var_Thr")
head(df)
```


Напишите код, который вберет из датафрейма `df` только те строки, в которых переменная `Var_One` принимает значения `i, c, f`.

Должен получиться вот такой результат

```{r}
df[df$Var_One %in% c("i", "c", "f"), ]
```


Напишите код, который выберет из датафрейма `df` только переменные `Var_One` и `Var_Thr`

Должен получиться вот такой результат (возможно несколько решений)

```{r}
df[ , c("Var_One", "Var_Thr")]
```

Напишите код, который добавит в датафрейм переменную Var_Four, которая будет равна квадрату значений переменной Var_Two.  Должен получиться вот такой результат.


```{r}
df$Var_Four <- df$Var_Two^2
df
```

